<%= form_with(model: picture_upload, local: true) do |form| %>
  <% if picture_upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(picture_upload.errors.count, "error") %> prohibited this picture_upload from being saved:</h2>

      <ul>
      <% picture_upload.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
